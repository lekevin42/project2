import Ember from 'ember';

export default Ember.Route.extend({
/*  model() {
    this.get('store').push({
      data: [{
        id: 1,
        type: 'image',
        attributes: {
          canvas: 'NOTLIKETHIS'//new createjs.Stage("demoCanvas")
        },
        relationships: {}
      }]
    });
  }
*/
});
//console.log('pushed');
